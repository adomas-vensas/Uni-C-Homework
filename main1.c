#include <stdio.h>
#include <stdlib.h>
#include "slluti.h"

int main()
{
    Node *head;
    Node *jeff;
    Node *num1, *num2, *num3;
    double h = 10, j = 9, k = 8;

    //createList
    printf("%d ", head);
    createList(&head);
    printf("%d",  head);


    // createList(&jeff);
    // createList(&jeff);
    // createList(&head);
    // createList(&head);


    // printf("%d", getListSize(&jeff));


    // printf("%d", &head);
    // printf("%p", head -> value);
    // insertElement(&head, 1, &h, sizeof(double));
    // insertElement(&head, 2, &j, sizeof(double));
    // insertElement(&head, 3, &k, sizeof(double));
    // deleteList(&head);
    // printf("%d\n", getListSize(&head));
    // deleteElement(&head, 1);

    // insertElement(&head, 1, &j, sizeof(double));
    // insertElement(&head, 2, &k, sizeof(double));

    // insertElement(&head, 3, &h, sizeof(int));
    // insertElement(&head, 4, &h, sizeof(int));

    // k = *((double *)getElementValue(&head, 3));
    // insertElement(&head, 2, "Gyvenimas be ribu", 18 * sizeof(char));
    // createList(&head);
    // deleteList(&head);
    // createList(&head);

    // insertElement(&head, 10, 2);

    // createList(&head);
    // // insertElement(&head, 20, 1);
    // // deleteElement(&head, 0);
    // printf("List size: %u\n", getListSize(&head));
    // // printf("%d", getElementValue(&head, 0));

    // printList(jeff);
    // reverseList(&head);
    // printList(&head, printDouble);
    // printList(&head, printDouble);




    return EXIT_SUCCESS;
}
